extends layout

block content
    for group in groups
        table.table.table-striped
            for person in group
                tr
                    td #{person.email}
                    td #{person.group}